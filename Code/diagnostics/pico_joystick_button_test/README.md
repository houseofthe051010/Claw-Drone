# Pico joystick diagnostic

Run `main.py` directly with Thonny or `mpremote` to print raw and normalized
joystick values plus the two stick-button states. This diagnostic does not
start UART or ESP-NOW communication and does not exercise the additional
GP0-GP11 trim and servo buttons.

Do not leave this file installed as `/main.py` when operating the aircraft.

